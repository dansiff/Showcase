import type { ReactNode } from "react";

// Standalone layout for italian showcase - no default header/footer
export default function ItalianLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
