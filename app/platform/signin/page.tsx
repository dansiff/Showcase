export const metadata = {
  title: "Sign In — FusionSpace",
  description: "Sign in to your creator or fan account",
};

import PlatformSigninForm from "./PlatformSigninForm";

export default function PlatformSignInPage() {
  return <PlatformSigninForm />;
}
